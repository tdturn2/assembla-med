# Same-origin /api (Nitro proxies to Nest). Recommended for local + staging.
NUXT_PUBLIC_API_BASE=/api
# Nest origin only (no /api suffix). Local default is in nuxt.config.
NUXT_API_PROXY_TARGET=http://127.0.0.1:4000
